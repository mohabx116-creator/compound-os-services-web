export const ROUTES = {
  HOME: '/',
  SERVICES: '/services',
  COMMUNITY: '/community',
  SERVICE_CATEGORY: '/services/categories/:slug',
  SERVICE_DETAIL: '/services/items/:slug',
  SERVICE_REQUEST: '/services/items/:slug/request',
} as const;
